package game.world.map;

/**
 * Enum is representing different types of shapes for object in map in 2d space.
 * This should be used for collision detection.
 */
public enum ShapeType {
    TRIANGLE, RECTANGLE
}
